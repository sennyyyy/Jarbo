# ALPHA Jarbo 1.0.9 Manual Smoke Test

Release status: **Parts A and C complete; Part B contains release-blocking lifecycle failures; Part D is incomplete with D02/D05 failures; Part E is in progress with E01 failing critically and E02–E04 passing.**

Decision while any blocking row is incomplete: **HOLD**

This checklist is for the designated v1.0.9 release-candidate app, not a debug run from a different commit. Record one result per row in `docs/V1.0.9-FEATURE-AUDIT.md` and attach a log, screenshot, video, Instruments trace, or written observation where requested. Source inspection does not count as a manual pass.

## Environment record

| Field | Evidence |
|---|---|
| Tester and date | Codex-assisted rehearsal and human Part A checks, 2026-07-19 |
| Commit and build | `58246c2`; ALPHA Jarbo 1.0.9 (9) |
| App artifact SHA-256 | `dist/Jarbo-1.0.9-RC-58246c2.zip`: `d02b23d6df4794f4090e7b9d35c6dc4b31c4c6e29b8b694d2b487a6b44b0c0b2` |
| Mac model / CPU / RAM | MacBookPro18,3 / Apple M1 Pro (8 cores) / 16 GB |
| macOS / Xcode / Swift | macOS 26.5.2 / Xcode 26.6 / Swift 6.3.3 |
| Built-in or external camera | Not opened during Part A; hardware type still TBD |
| Display/Space arrangement | One 1800×1130 captured desktop; Space arrangement TBD |
| Accessibility / Camera / Automation state | HUD reports Control Ready; isolated profile Camera Off with no permission prompt; Automation TBD |
| Spotify installed and signed in | Not found in `/Applications`; sign-in state not tested |
| Evidence folder or issue link | `docs/evidence/v1.0.9/`; canonical installed app recorded in `CANONICAL-TEST-INSTALL.md` |

Use `Pass`, `Pass with limitations`, `Fail`, or `Blocked`. A safety failure, held input, hidden continued camera capture, crash, configuration loss, or unknown capability execution is release-blocking.

## A. Build and clean launch

| ID | Test | Expected result | Result | Evidence / issue |
|---|---|---|---|---|
| A01 | From a fresh clone, run `./verify.sh`. | Preflight, Swift tests, universal build, plist, signature, and app-bundle checks all pass. | Pass | Fresh clone of `58246c2`: 13/13 tests; signed x86_64/arm64 app. Log: `docs/evidence/v1.0.9/part-a/A01-clean-verify-58246c2.log`. Exact-candidate CI: [Verify Jarbo #4](https://github.com/sennyyyy/Jarbo/actions/runs/29677667695). |
| A02 | Launch the verified app from Finder. | One Jarbo process/window launches; no immediate crash or duplicate window. | Pass | LaunchServices clean launch (the `open` equivalent of double-click) produced one process and one visible HUD with no immediate crash. `A02-clean-launch.png`; `A02-process-observation.txt`. A later non-crashing `AttributeGraph` warning is tracked separately as low-severity JARBO-109-002. |
| A03 | Inspect Dock, Command-Tab, and Mission Control/F3. | Jarbo behaves as a regular app and its open window appears in expected switchers/overview. | Pass | Human check confirmed Jarbo appears in the Dock, Command-Tab switcher, and Mission Control/F3; selection returns to one HUD without a duplicate window. Written observation in `PART-A-OBSERVATIONS.md`. |
| A04 | Observe the first launch with no saved camera preference. | Camera is Off; Jarbo does not begin capture until explicitly enabled. | Pass | Launched with an isolated empty `CFFIXED_USER_HOME`; the HUD showed `Camera Off` and `Camera Offline`, with no macOS camera privacy indicator. `A02-clean-launch.png`. |
| A05 | Open the menu-bar item in light and dark appearance. | The distinct template Jarbo icon is legible at normal scale, accessible as “Jarbo controls,” and is not confused with camera privacy UI. | Pass with limitations | Human check passed the light/dark icon, menu contents, `Jarbo controls` accessibility label, camera-indicator distinction, stability, and single-window/process checks. **Defect JARBO-109-001:** `Quit Jarbo` is present but disabled/grey and cannot be clicked. Written observation in `PART-A-OBSERVATIONS.md`. |

## B. Window, HUD, and background lifecycle

| ID | Test | Expected result | Result | Evidence / issue |
|---|---|---|---|---|
| B01 | Hide and show the HUD from the menu bar and configured HUD action. | Visibility changes exactly once and the menu label stays accurate. | Pass | Human tester reported expected hide/show behavior with no duplicate HUD. |
| B02 | Close the HUD window, then use Dock, Command-Tab, Mission Control where applicable, and menu-bar Show. | Closing does not quit; each supported restore path reopens one HUD predictably. | Fail | Human tester reported the red close/reopen lifecycle terminates Jarbo. Crash report `Jarbo-2026-07-19-210821.ips` confirms `EXC_BAD_ACCESS` in `AppDelegate.applicationShouldHandleReopen`. JARBO-109-004. |
| B03 | Keep another app focused while hand status and menu state update. | Jarbo does not activate or steal keyboard focus. | Pass | Human tester reported focus remained with the other app. Written observation; no screen recording attached. |
| B04 | Enter full screen in video/presentation/game-like content and change Spaces. | Jarbo's normal-level window does not force itself above content or every Space. | Pass | Human tester reported expected full-screen and Space behavior. Written observation. |
| B05 | Hide the HUD for 30 minutes with the explicitly intended services active. | Menu access and intended background controls continue; no unexpected HUD activation or focus steal. | Pass with limitations | Human tester reported expected background behavior; required Activity Monitor/Instruments CPU and memory evidence was not attached. |
| B06 | Quit from the menu bar while no input is held, then relaunch. | Process exits, camera releases, and saved intended settings restore. | Fail | Cannot begin: menu-bar `Quit Jarbo` is disabled. Defect JARBO-109-001; termination/relaunch behavior remains untested. |

## C. Camera lifecycle and privacy

| ID | Test | Expected result | Result | Evidence / issue |
|---|---|---|---|---|
| C01 | Select Camera On from Off. | Permission is requested only if necessary; one session starts; live state becomes On; macOS privacy indication is visible. | Pass | Human tester confirmed one live feed and the macOS camera privacy indicator before the C03 saved-On relaunch. No duplicate session or unexpected action was reported. |
| C02 | Select Camera Off while tracking. | Capture/frame processing stop, preview and stale observations clear, candidates cancel, pointer/buttons release, and state becomes Off. | Pass | Human tester reported all C02 checks passed. Written observation; video/privacy-indicator timing not attached. Full held-button interruption remains separately covered by Part E. |
| C03 | Relaunch once with saved intent Off and once with saved intent On. | Off remains inactive; On starts only according to saved intent and live permission/resource state is explicit. | Pass | Saved-Off relaunch remained inactive. Saved-On relaunch preserved intent and resumed exactly one live session/process; human tester confirmed Camera On and the privacy indicator. See `part-c/C03-saved-off-relaunch.log` and `C03-saved-on-relaunch.log`. |
| C04 | Toggle Camera On/Off ten times. | No duplicate session, stale gesture, stuck control, runaway CPU, or repeated memory growth; hardware releases after each Off. | Pass with limitations | Human tester completed 10 cycles with one surviving process and no duplicate/stale/stuck behavior. RSS peaked at 260,624 KB and settled to 112,496 KB, below the 150,592 KB start, so no repeated-growth pattern was observed. Camera-Off CPU remained about 49–55%; JARBO-109-005. Logs and summary under `part-c/C04-*`. |
| C05 | Deny Camera, retry, then grant it in System Settings. | Jarbo remains stable, explains denial, and can recover without relaunch or duplicate sessions. | Pass with limitations | Denial was handled safely and the recovery link opened the correct settings page. The human tester confirmed one live session after granting access, but the process changed from PID 65214 to 66409 during recovery. No matching July 20 crash report was found; nevertheless, in-process recovery without relaunch was not demonstrated. JARBO-109-006; `part-c/C05-permission-reset.log`. |
| C06 | Revoke Camera while active. | Capture stops/fails safely, active controls release, and state/recovery guidance are accurate. | Pass | Human tester confirmed the live feed/privacy indicator stopped, overlays and candidates cleared, no action fired, and accurate recovery guidance appeared. Jarbo remained on PID 66409 with one process and no new crash report. Saved Camera-On intent remained enabled as expected while live authorization was revoked. `part-c/C06-runtime-revocation.log`. |

## D. Tracking and HUD feedback

| ID | Test | Expected result | Result | Evidence / issue |
|---|---|---|---|---|
| D01 | Show left hand, right hand, then both. | Selfie orientation is understandable; labels match the physical hands; roles do not immediately swap. | Pass | Human tester confirmed physical left-only, right-only, and both-hand checks: labels were anatomically correct, roles stayed stable, selfie orientation was understandable, and the preview was not stretched. Written observation; requested video was not attached. `part-d/D01-handedness-observation.md`. |
| D02 | Perform representative static, motion, orientation, unknown, and contact poses. | A green box/skeleton and readable hand/gesture/action label track each detected hand without stretching the preview. | Fail | Overlay alignment/aspect and action suppression passed. Peace detected about half the time; Swipe Up never detected; palm-facing-camera produced No Gesture while the back of the hand produced Open Palm; ambiguous Unknown became Finger Heart about half the time; thumb/index passed; thumb/middle and thumb/ring worked only after first opening the palm. JARBO-109-007/008/009. Written observation; requested video not attached. `part-d/D02-gesture-observation.md`. |
| D03 | Remove each hand while a candidate is visible. | Hand disappears promptly; candidate clears and no delayed action fires. | Pass | Human tester confirmed left removal, right removal, one-at-a-time removal from a two-hand view, and both-hand removal all cleared promptly with no delayed actions. Written observation in `part-d/D03-removal-observation.md`; active held-input release remains Part E. |
| D04 | Test normal/low lighting, fast motion, and partial occlusion. | Low-quality/incomplete observations reject safely; no unsafe click or command occurs. | Pending | Deferred at the human tester's request because lighting conditions could not currently be tested. No result inferred; run all D04 conditions together later. `part-d/D04-DEFERRED.md`. |
| D05 | Cross two hands and uncross them. | Any identity uncertainty resolves safely; no wrong-hand consequential action or stuck input. | Fail | Separated labels and post-uncross recovery passed, with no ghost/stuck overlay or dispatched action while controls were paused. During close/brief occlusion only one hand remained, handedness labels flickered rapidly, and unspecified consequential false gesture labels appeared. JARBO-109-010. Written observation; video not attached. `part-d/D05-crossed-hands-observation.md`. |

## E. Pointer and mouse-button lifecycle

| ID | Test | Expected result | Result | Evidence / issue |
|---|---|---|---|---|
| E01 | Use the assigned pointer hand at 0.5× sensitivity and clutch/reposition. | Pointer is controllable, does not require an outstretched camera-edge reach, and resumes without a large jump. | Fail | On the built-in display, 0.50× movement, clutch pause, and no-jump resume passed with slight jitter. The tester reports that index-finger detection is very poor and the pointer barely works reliably; activation is angle-dependent and camera-edge reach remains necessary (JARBO-109-011). A pointer-only follow-up produced 7 unintended Left down/up and 3 unintended Right down/up events; Middle remained 0/0 and every button ended released. This is Critical JARBO-109-012. Tester prefers direct proportional mapping with lower smoothing/sensitivity. Video not attached; `part-e/E01-pointer-observation.md` and `E01-pointer-event-counters.md`. |
| E02 | Change sensitivity, quit, and relaunch. | Movement response changes and the setting persists. | Pass | At 0.70×, human tester confirmed a changed and better response, with 0.75–0.90× preferred; all click bindings were disabled and pad counters stayed 0. A normal-profile quit/relaunch changed PID 15349 to 23491 with exactly one canonical process. Config and post-relaunch HUD both retained 0.70×, and all three click bindings remained disabled. 0.70× intentionally retained during testing. `part-e/E02-sensitivity-persistence.md`. |
| E03 | Thumb/index pinch and release over a harmless target. | One left down begins, stays held while pinched, and one left up occurs on separation. | Pass | With only Primary click enabled on the local event pad, the human tester confirmed zero early events, exactly one Left down during the two-second thumb/index hold, no repeat or Right/Middle collision, and exactly one Left up on separation. Final state released; Primary click disabled again. Written counter observation; video not attached. `part-e/E03-left-button-lifecycle.md`. |
| E04 | Hold thumb/index pinch and move. | A continuous left drag occurs only for the hold duration. | Pass | With only Primary click enabled on the local pad, one Left down initiated a continuous drag; counts stayed 1/0 while held, one Left up stopped the drag immediately, the target did not follow afterward, no wrong/repeated events occurred, and final state was released. Primary click disabled again. Written counter observation; video not attached. `part-e/E04-left-drag-lifecycle.md`. |
| E05 | Thumb/middle hold and release. | One right-button lifecycle occurs with no left-click collision. | Pending | |
| E06 | Thumb/ring hold and release. | One middle-button lifecycle occurs with no fist/pinch collision. | Pending | |
| E07 | While holding each button in turn, remove the hand, open Actions, choose Camera Off, and quit. | Every interruption releases immediately enough to prevent a stuck button. | Pending | Four-path video or event trace. |
| E08 | Present a fist and other close configurations without intended thumb contact. | They do not begin a mouse-button hold. | Pending | |

## F. Gesture actions and permissions

Configure harmless bindings and use deliberate edge-triggered activations. For every row, verify the configured hand and gesture, one begin/end lifecycle where relevant, visible success/failure, and no repetition during a held static pose.

| ID | Action(s) | Expected result | Result | Evidence / issue |
|---|---|---|---|---|
| F01 | Desktop left / Desktop right | Moves exactly one Space in the configured direction; permission/shortcut failure is explained. | Pending | At least two Spaces required. |
| F02 | Mission Control / App Exposé / Show Desktop | Opens each macOS surface exactly once. | Pending | |
| F03 | Volume up / down / mute | Changes system audio predictably without runaway repeats. | Pending | |
| F04 | Play/pause / next / previous | Controls Spotify by default exactly once per activation, or reports the unavailable target. | Pending | |
| F05 | Open URL | Opens a valid `https` URL; rejects empty, malformed, and non-http(s) values. | Pending | Use a harmless local/test URL. |
| F06 | Open application | Opens/focuses an existing app; a missing app reports a useful failure. | Pending | |
| F07 | Open file | Opens an existing harmless file; missing/invalid path reports failure. PDF understanding is not tested. | Pending | |
| F08 | Web search | Opens one correctly encoded query. | Pending | |
| F09 | Display note | Shows the exact saved note once and preserves it. | Pending | |
| F10 | Speak text | Speaks explicitly saved text or reports unavailable speech output; speech-command input is excluded. | Pending | |
| F11 | Toggle HUD | Hides/shows without losing menu-bar control or stealing focus on passive show. | Pending | |
| F12 | Generate image | Starts only the explicitly configured workflow and clearly reports a missing key/network/dependency. Image understanding is excluded. | Pending | Avoid paid request unless authorized. |
| F13 | Shell command | Runs one explicitly saved harmless command; empty/invalid value is blocked; failure is reported. | Pending | Use `true` or another no-side-effect command. |
| F14 | Cancellation / pause controls | Pausing or cancelling prevents new gesture actions and releases held input; resuming restores only deliberate future actions. | Pending | |
| F15 | Accessibility denied, granted, then revoked | Output is blocked with recovery guidance, recovers safely after grant, and stops after revocation. | Pending | |
| F16 | Spotify Automation denied then granted | Media-control failure/recovery is understandable and never reports a false success. | Pending | |

## G. Actions editor and persistence

| ID | Test | Expected result | Result | Evidence / issue |
|---|---|---|---|---|
| G01 | Open Actions while Camera is active and an input is nearly/actively held. | Gesture action dispatch pauses, held controls release, and only landmark work needed for training remains. | Pending | |
| G02 | Add a binding, rename it, change hand, gesture, action, and value. | One stable row updates responsively and shows appropriate fields/validation. | Pending | |
| G03 | Disable, re-enable, test, reorder where supported, and remove the row. | Disabled cannot execute; test affects only that row once; order/removal persists without adjacent corruption. | Pending | |
| G04 | Enter invalid and empty values for every value-bearing action. | Clear inline/execution error; no side effect. | Pending | URL, app, file, search, shell, speech, note, image. |
| G05 | Perform ten open/edit/save/close cycles. | No duplicate/lost rows, crash, held input, sustained stall, or repeated memory growth. | Pending | Instruments/signpost evidence. |
| G06 | Change theme, hand roles, sensitivity, bindings, samples, notes, camera intent, and intended HUD state; quit normally. | Intended persisted fields restore accurately after relaunch. | Pending | Note whether HUD visibility is intentionally ephemeral. |
| G07 | Restore essentials. | Safe left-pointer/right-controls roles and essential bindings return; held input is not left active. | Pending | |
| G08 | Test a malformed/incomplete configuration in an isolated profile. | Missing fields use safe defaults; corrupt data is backed up; Jarbo remains usable. | Pending | Preserve original user profile. |

## H. Personalized training and model lifecycle

| ID | Test | Expected result | Result | Evidence / issue |
|---|---|---|---|---|
| H01 | Select a hand with camera off, missing hand, incomplete/stale hand, then fresh complete hand. | Capture fails with a specific reason until 21/21 fresh landmarks exist; no stale sample is saved. | Pending | |
| H02 | Save ten varied `No gesture / other` samples. | Guidance offers relaxed/curled/transition/unrelated examples; count is exact; repeated near-identical captures warn. | Pending | |
| H03 | Capture 9 then 10 samples for one eligible static class. | Checklist names exact missing count; the class becomes complete only at 10. | Pending | |
| H04 | Add ten dynamic and ten orientation samples with only one complete static class. | These samples do not enable the static Core ML build. | Pending | |
| H05 | Complete a second eligible static class. | Build enables only with No Gesture 10/10 and both static classes 10/10; incomplete extras are safely excluded. | Pending | |
| H06 | Build the model while observing UI responsiveness and log. | Training runs without freezing ordinary UI; per-class diagnostics and final Ready/failure persist after closing/reopening Actions. | Pending | |
| H07 | Quit/relaunch and recognize trained static classes. | Compiled model reloads locally and readiness/status remain understandable. | Pending | Accuracy is recorded, not assumed. |
| H08 | Cause or simulate a failed rebuild while a usable model exists. | Failure reason persists and any previously usable model is not silently lost or misreported. | Pending | Use a safe isolated profile. |
| H09 | Delete model with confirmation. | Model artifacts are removed, samples remain unless separately cleared, and fallback behavior/status are clear. | Pending | |
| H10 | Rebuild after deletion. | A new valid model loads without app restart or stale metadata. | Pending | |

## I. Included HUD surfaces

| ID | Test | Expected result | Result | Evidence / issue |
|---|---|---|---|---|
| I01 | Launch intro and Arc Reactor visual. | Animation completes without blocking controls or leaving sustained load. | Pending | |
| I02 | Switch Arc Reactor, Midnight, Crimson, and Matrix themes. | Each remains readable and persists after relaunch. | Pending | Four screenshots. |
| I03 | Drag supported HUD widgets, hide/show HUD, and relaunch. | Dragging is responsive; document which layout state is intentionally persisted. | Pending | |
| I04 | Camera feed and hand overlay. | Feed, aspect, green boxes, landmarks, and labels remain aligned and readable. | Pending | |
| I05 | System status, time, battery, and location/map where available. | Current values display or the unavailable/permission state is clear; no crash. | Pending | |
| I06 | Notes/text and music controls. | Note display and media controls work or report a clear unavailable target. | Pending | |
| I07 | Audio visualizer without voice-command testing. | Visualizer starts/stops safely and does not degrade gesture control when unused. | Pending | Mic permission may block. |

## J. Performance and soak gates

| ID | Test | Target | Result | Evidence / issue |
|---|---|---|---|---|
| J01 | Actions open-to-interactive | at most 500 ms | Pending | Signpost/Instruments trace. |
| J02 | Common setting edit feedback | at most 100 ms | Pending | Trace/video measurement. |
| J03 | Longest ordinary editor stall | no sustained stall over 100 ms | Pending | Main-thread trace. |
| J04 | CPU: HUD idle, camera active, Actions open | no sustained editor increase above active-camera baseline | Pending | Same Mac and sampling interval. |
| J05 | Memory before/after and ten editor cycles | returns near baseline; no repeated-growth pattern | Pending | Allocations/Activity Monitor. |
| J06 | Settings writes during ten seconds of continuous edit | debounced; no per-keystroke full write | Pending | File/system trace. |
| J07 | Actions/settings soak | 30 minutes, no crash, held input, runaway CPU, or material growth | Pending | Trace + observation. |
| J08 | Two-hand active session | 30 minutes, no unsafe role swap or stuck control | Pending | Video/log. |

## K. Excluded-feature safety

| ID | Excluded feature | Required result | Result | Evidence / issue |
|---|---|---|---|---|
| K01 | Voice-command reliability | Disabled/unavailable/experimental behavior cannot crash or degrade non-voice controls. | Pending | |
| K02 | PDF reading/understanding | Ordinary file opening remains safe; unsupported understanding is clear. | Pending | |
| K03 | Image reading/understanding | Image generation and unrelated HUD actions remain safe; unsupported analysis is clear. | Pending | |
| K04 | 3D model viewer | When unused, it creates no sustained background load or unrelated regression. | Pending | |

## Sign-off

| Gate | Decision / evidence |
|---|---|
| Critical/high defects | TBD |
| Safety release paths | TBD |
| Clean checkout and CI | TBD |
| Included feature audit | TBD |
| Performance and soak | TBD |
| Known limitations reconciled | TBD |
| Reviewer / date / commit | TBD |

Final decision: **HOLD until all blocking evidence is recorded.**
